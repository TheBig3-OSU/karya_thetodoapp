# Copy to .env.local for local development. Vite only exposes variables
# prefixed with VITE_ to the app. On Vercel, set these in the dashboard
# (scoped per environment) instead of committing env files.
VITE_API_URL=http://localhost:8000
